<?php
/**
 * Archived import helper — moved to archive after Excel removal.
 * This script requires PhpSpreadsheet and reads .xlsx files to import data
 * into the current MySQL schema. It's kept here for historical reference.
 *
 * If you need to re-enable it, restore `phpoffice/phpspreadsheet` and run
 * the original `cli/import_excel.php` script.
 */

echo "Legacy import helper (archived). To run it, restore `phpoffice/phpspreadsheet` and use the original script in `cli/import_excel.php`\n";

?>
