services:
  app:
    build: .
    container_name: app
    working_dir: /app
    ports:
      - "8080:8080"
    volumes:
      - .:/app:cached
    environment:
      - TZ=UTC
      - DB_HOST=db
      - DB_PORT=3306
      - DB_NAME=app
      - DB_USER=root
      - DB_PASS=example
    depends_on:
      - db
  db:
    image: mariadb:10.5
    environment:
      MYSQL_ROOT_PASSWORD: example
      MYSQL_DATABASE: app
    ports:
      - "3306:3306"

